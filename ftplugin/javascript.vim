" my tweaks for js indentation
setlocal tabstop=2 " number of spaces that <Tab> in file uses
setlocal softtabstop=2 " number of spaces that <Tab> uses whie editing
setlocal shiftwidth=2 " indenty by x spaces
